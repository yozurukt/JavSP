#!/usr/bin/env bash

export UV_CACHE_DIR="$PWD/.uv-cache"

if [[ ! -d .venv ]]; then
  log_error 'No .venv found. Run `UV_CACHE_DIR=.uv-cache uv venv --python 3.12` first.'
  exit 2
fi

export VIRTUAL_ENV="$PWD/.venv"
PATH_add "$VIRTUAL_ENV/bin"
